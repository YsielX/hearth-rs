return {api_version=1,id="EX1_561",name="Alexstrasza",text="<b>Battlecry:</b> Set a hero's remaining Health to 15.",set="EXPERT1",type="minion",rarity="legendary",cost=9,attack=8,health=8,tags={"dragon"},keywords={"battlecry"},target_mode="required_if_available",targets=function(ctx)local r={};for _,e in ipairs(ctx:characters())do if ctx:entity(e).type=="hero"then r[#r+1]=e end end;return r end,on_battlecry=function(ctx,self,target)if target then ctx:set_health(target,15)end end}
