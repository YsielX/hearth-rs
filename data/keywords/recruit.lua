-- Card text selects a deck entity and calls ctx:recruit; the engine owns reservation,
-- cancellation, positioning, and summon events.
return { api_version = 1, module_type = "keyword", id = "recruit", name = "Recruit" }
