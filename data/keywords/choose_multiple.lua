-- A board aura marker consumed by choose_one.lua. This keeps the interaction
-- between Fandral and every Choose One card in Lua instead of naming Fandral
-- in the engine.
return {
    api_version = 1,
    module_type = "keyword",
    id = "choose_multiple",
    name = "Choose Multiple",
}
