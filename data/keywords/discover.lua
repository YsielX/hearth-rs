-- The card defines its candidate pool; ctx:discover_* owns sampling, deduplication,
-- choice presentation, and deterministic continuation.
return { api_version = 1, module_type = "keyword", id = "discover", name = "Discover" }
