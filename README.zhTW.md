# hearth-rs

[English](README.md) | [簡體中文](README.zhCN.md) | [繁體中文](README.zhTW.md)

一個 Rust 權威規則核心 + Lua 卡牌/關鍵詞規則層的爐石命令列原型。

核心目標是：新增卡牌只增加 Lua 檔案，不註冊 Rust ID，也不在 Rust 中為卡名或關鍵詞寫分支。

## 當前結構

```text
data/
├── hero_powers/           # 每個英雄技能一個獨立 Lua 模組
├── hearthstonejson/       # 已實現定義的官方後設資料來源快照
├── keywords/              # 嘲諷、聖盾、突襲等獨立 Lua 模組
├── locales/               # enUS / zhCN / zhTW 官方名稱與正文
└── sets/                  # 按 HearthstoneJSON set 存放的官方卡牌
crates/
├── hearth-core/           # 狀態機、區域、事件佇列、確定性 RNG、replay
├── hearth-script/         # Lua 沙箱、模組載入、規則鉤子與效果橋接
├── hearth-app/            # CLI/GUI 共用的對局工作階段、牌組庫與應用服務
├── hearth-cli/            # `play` 和 `fuzz` 子命令
├── hearth-client-bevy/    # Bevy 0.19 原生圖形客戶端
├── hearth-bot/            # 不讀取隱藏資訊的確定性基礎 Bot
└── hearth-fuzz/           # 狀態機 Fuzzer 函式庫（無獨立二進位）
decks/demo.json            # 官方卡演示牌組
decks/quest_rogue.json     # Dog 2017 經典洞穴任務賊
```

Rust 負責不能交給指令碼隨意修改的原子能力：實體身份、區域容器、法力支付、攻擊/傷害提交、死亡檢查點、效果佇列、輸入暫停、確定性隨機、事務回滾與 replay。

Lua 負責卡牌語義和關鍵詞語義：目標選擇、戰吼、亡語、奧秘、發現、觸發條件、觸發效果，以及攻擊規則修飾。Rust 引擎裡不再按 `"taunt"`、`"divine_shield"`、`"reborn"` 等字串執行具體規則。

`hearth-app` 是 CLI 與圖形客戶端共用、且不依賴具體 UI 技術的應用層。controller-neutral 的 `MatchSession` 統一負責 runtime/牌組建構、sideboard、種子先手、replay/snapshot 和公開檢視；`GameSession` 在其上增加熱座與 Bot 策略。公共事件/操作文字和逾時策略也在這裡共用，終端 I/O 與 Bevy ECS/渲染仍由各前端負責。

## 官方卡牌資料

目前倉庫保留 1999 個官方卡牌、衍生物、英雄和英雄技能定義，覆蓋 46 個 set；其中包括完整的 30 張《納克薩瑪斯的詛咒》、123 張《哥哥打地地》、31 張《黑石山的火焰》、132 張《銀白聯賽》、45 張《探險者協會》、134 張《古神碎碎念》、45 張《夜夜卡拉贊》、132 張《黑街英雄之加基森風雲》、135 張《勇闖安戈洛》、135 張《冰封王座的騎士》和 26 張《阿薩斯之路》，以及 11 個標準基礎英雄及其職業英雄能力、每個已審計構築關鍵詞至少一張完整實作的官方代表牌，並完整實作一套經典洞穴任務賊。這仍是代表性規則語料，不是完整官方卡池。

名稱、正文、數值、官方 ID 和 set 來自 HearthstoneJSON 的客戶端資料。預設英文來源快照位於 [data/hearthstonejson/selected.enUS.json](data/hearthstonejson/selected.enUS.json)，三語顯示文字位於 `data/locales/`，取數說明見[繁體中文取數文件](data/hearthstonejson/zhTW/README.md)。不可收集衍生物也使用官方 ID，例如鬼靈蜘蛛 `FP1_002t`，不再使用自造的 `TOKEN_*` ID。

## 三語文字

CLI 的 `--locale` 接受 `enUS`、`zhCN` 和 `zhTW`，未指定時預設英文。它會切換卡牌名稱、正文、幫助、狀態標籤、事件、錯誤以及 Lua 動態選項提示；命令本身保持穩定的英文關鍵字，便於 replay 和指令碼複用。牌組名是使用者隨意填寫的後設資料，始終原樣顯示唯一的 `name` 值，不參與 locale。

```bash
cargo run -p hearth-cli -- play --locale zhTW
cargo run -p hearth-cli -- play --locale enUS
```

卡牌 Lua 中的英文名稱和正文是預設後備值。正式卡包透過官方 ID 從 `data/locales/<locale>.json` 合併顯示文字，缺少任一支援語言會被測試拒絕。動態提示使用 `ctx:localize(enUS, zhCN, zhTW)`。

## 關鍵詞也是 Lua

卡牌只引用關鍵詞模組：

```lua
return {
    api_version = 1,
    id = "GVG_085",
    name = "Annoy-o-Tron",
    text = "<b>Taunt</b>\n<b>Divine Shield</b>",
    set = "GVG",
    type = "minion",
    cost = 2,
    attack = 1,
    health = 2,
    keywords = { "taunt", "divine_shield" },
}
```

`taunt` 對應 [data/keywords/taunt.lua](data/keywords/taunt.lua)，它透過通用 `attack_priority` 規則鉤子提高攻擊目標優先順序。`divine_shield` 對應獨立模組，透過 `damaged/before` 觸發器禁用自身並取消傷害事件。

構築模式的 68 個功能性關鍵詞均有獨立 Lua 模組。規則型關鍵詞由模組直接摺疊規則或監聽事件；效果詞由模組統一觸發條件和時序，並以載入期強制的卡牌 hook 承載該牌獨有的數值、目標或衍生物。完整口徑、覆蓋矩陣和不計入項見 [關鍵詞覆蓋表](docs/KEYWORDS.md)。組合這些模組不需要按卡牌 ID 修改 Rust。可交易模組透過通用 `can_trade` 規則開放玩家行動；鍛造、預備和泰坦能力透過通用 `card action` 介面開放玩家行動；CLI 分別使用 `trade <實體ID>` 和 `action <實體ID> <動作ID> [目標ID]`。

帶數值的關鍵詞也由 Lua 模組實現。閃電箭和土元素只宣告 `keywords = { "overload" }` 以及 `keyword_params = { overload = 1/2 }`；[data/keywords/overload.lua](data/keywords/overload.lua) 自己讀取引數並呼叫通用法力原語。被汙染的狂熱者同樣以 `spell_damage = 1` 引數引用 [data/keywords/spell_damage.lua](data/keywords/spell_damage.lua)，由 Lua 提供印刷法強基礎值，再進入 Rust 通用屬性分層和法術傷害結算。

卡牌的隱藏官方規則也留在 Lua。例如野性成長在未滿 10 個水晶時呼叫通用加水晶原語，達到上限時改為生成官方 `CS2_013t`“法力過剩”；該衍生法術自己的抽牌邏輯也與主卡放在同一個 Lua 檔案中。

玩家職業是 Rust 權威對局狀態，但發現池仍由卡牌 Lua 構造。幽靈寫手、劇毒魔蠍和甲蟲鑰匙鏈讀取 `ctx:player(player).class`，只把該職業或中立的合格定義交給通用確定性發現原語；職業會寫入 replay 和 snapshot。

## 新增卡牌

在 `data/sets/<set>/` 增加 Lua 檔案即可。比如一張目標傷害法術：

```lua
return {
    api_version = 1,
    id = "MY_SET_001",
    name = "示例法術",
    text = "造成3點傷害。",
    set = "MY_SET",
    type = "spell",
    cost = 2,
    target_mode = "required",

    targets = function(ctx, self)
        return ctx:enemy_characters(self)
    end,

    on_play = function(ctx, self, target)
        cardlib.effects.damage(ctx, target, 3)
    end,
}
```

`target_mode = "required"` 用於沒有合法目標便不能打出的法術/英雄技能；帶目標戰吼使用
`"required_if_available"`，有合法目標時必須選擇，沒有時仍可打出且 `on_battlecry` 收到 `nil`。
`"optional"`（預設值）允許省略目標。目標規則本身仍由 Lua 的 `targets` 函式定義。

重啟程式後會自動遞迴發現。只要效果能由現有通用查詢、事件鉤子和效果原語表達，新增卡牌無需修改任何 Rust 程式碼。完整介面見[繁體中文 Lua 卡牌 API](docs/zhTW/CARD_API.md)。

## 執行

需要 Rust 1.88 或更新版本。Lua 5.4 由 `mlua` 的 `vendored` 功能構建。

```bash
cargo run -p hearth-cli -- play \
  --deck-one decks/demo.json \
  --deck-two decks/demo.json \
  --seed 42
```

### Bevy 圖形用戶端

執行原生 Bevy 0.19 用戶端：

```bash
cargo run -p hearth-client-bevy
```

主選單可進入牌組選擇、「我的收藏」與設定，也可以透過「離開遊戲」正常關閉用戶端。設定頁可立即切換英語、簡體中文、繁體中文、
關閉或調整預設回合計時，並支援視窗/無邊框全螢幕及 80%、100%、120% 介面縮放；任何介面按
`F11` 也可切換無邊框全螢幕。設定會以原子方式儲存至
`$XDG_CONFIG_HOME/hearth-rs/client.json`（未設定 XDG 時為
`~/.config/hearth-rs/client.json`），重新啟動後自動恢復；既有 v1/v2 設定會保留原有選項並自動
遷移，新增 AI 難度預設為「普通」。`--fullscreen`、`--windowed`、`--ui-scale 80|100|120`、
`--locale`、`--turn-seconds` 與 `--bot-difficulty easy|normal|hard` 可覆蓋已儲存值，
`--settings PATH` 可指定隔離設定檔，`--no-settings` 可停用。

牌組選擇器與收藏編輯器中的「牌組代碼」頁面可匯出完整構築牌表，也可透過系統剪貼簿
匯入官方用戶端複製的多行文字或裸 Deckstring。匯入結果會先作為未儲存的自訂草稿開啟，
確認後才寫入 `decks/custom/`。匯入器會驗證模式、英雄、已實作卡牌的 dbfId、職業合法性、
牌組容量及複製上限。支援備牌的擁有者會使用官方 Deckstring 擴充格式編解碼；未實作的卡牌
仍會被明確拒絕。『樂團經理』精英牛頭大佬可在編輯器中組建三張卡牌的樂團，在對戰中發現一名
團員，選中的團員會在本局剩餘時間內從樂團移除。
雷納瑟王子會把同一編輯器與 Deckstring 流程擴充至 40 張卡牌，並在開局效果前把起始生命值設為
40；重播與快照會從提交的牌表重新推導這兩項規則。
死亡騎士卡牌透過 Lua 宣告結構化的血魄、冰霜、穢邪符文需求。應用層、Deckstring、權威對戰
建構器和精英牛頭大佬的備牌區都會執行官方三欄規則；Bevy 編輯器顯示目前最低符文承諾
（例如 `BB-`），並動態隱藏會使各顏色最高需求總和超過三欄的卡牌。
友方手下死亡會按同批死亡結算為死亡騎士的公開屍體資源；重生後再次死亡會再次計數，
而「復活的食屍鬼」等帶例外標記的衍生物不會留下屍體。Lua 提供精確消耗、最多消耗及
「成功消耗後繼續」的原子介面；運屍人、亡者大軍和解凍分別覆蓋獲得、受戰場容量約束的消耗、
衍生物例外與條件後續效果。CLI、Bevy、公開投影、重播/快照和 RL 觀察使用同一權威資源。

《泰坦諸神》瘟疫套件現已實作海拉、困苦的克瓦迪爾、殉船、叛墓者、統御者之杖、縛練守護者、
三種抽到時施放的瘟疫及不死農夫衍生物。
海拉會在瘟疫成功抽到並施放時立即把同種瘟疫重新洗入牌庫；被爆掉的瘟疫不會施放或重洗。冰霜瘟疫的
「下一張卡牌」加費可以疊加，並把結果封頂為 10。初始洗入和無盡重洗都會即時降低縛練守護者的手牌消耗。
無盡瘟疫是雙方可見的永久狀態，會進入公開投影、RL
觀察、重播/快照和 Bevy 戰場狀態列。
追悼者同時實作消耗屍體與鑄造後獲得屍體的形態；北境導覽會從牌堆發現並抽取實際實體，
再按官方法術派系判斷冰霜聯動；嚴寒凍結只鎖定對手由該牌直接抽到的兩張牌，直到其下個回合結束。
兵主實作三個帶目標或無目標的泰坦能力，按印刷符文需求建立對應發現池；其可疊加的下一張法術減費與
法術傷害也會進入重播並精確還原。
《奧杜爾的陷落》死亡騎士迷你系列另實作暗影符文、病弱的泥行者和邪惡靈魂牢籠，包括條件屍體消耗
以及複製完成增益後的手下完整狀態。
《阿薩斯之路》11 張冰霜卡牌現已全部實作，涵蓋同時作用於全部敵人的法術傷害、受上限約束的法力刷新、
凍結、下一張法術暫時減費、連續隨機傷害、按手牌法術派系計數與複製、受上限約束的屍體消耗，以及武器
最後一點耐久耗盡後仍會結算的攻擊後觸發。
《阿薩斯之路》血魄套件也實作嗜血之徒、惡毒血蟲、血魄轉化、沸血術、暗殞新人、窒息術和
奈幽蟲群護衛，涵蓋真實手牌實體目標、屍體條件手牌增益、符文發現池、最高攻擊並列隨機消滅、完整狀態
複製，以及可獨立疊加並被沉默移除的生命竊取感染。
其餘穢邪符文和無符文卡牌也已補齊，使《阿薩斯之路》26 張卡牌全部實作。瘟疫打擊、黑暗變形、墓地守衛、
穢邪狂熱、天譴軍團、屍體新娘、惡性恐魔和霜之哀傷涵蓋傷害致死後條件召喚、不死族目標變形、消耗屍體
賦予重生、從左到右強制戰鬥與全新副本重召、可重播隨機鋪場、動態無屍體衍生物、回合結束完整狀態複製，
以及最後一點武器耐久攻擊的擊殺記錄。
《巫妖王的進軍》13 張死亡騎士可收藏卡牌也已全部實作，包括屍爆逐波消耗並重用新產生的屍體、
莫格萊尼可疊加且跨回合持續的英雄傷害、可被沉默移除的死亡之聲感染、手牌內施法計數、指定友方手下的地標，
以及冰霜女王辛德拉苟莎的巨型附屬物與凍結消滅連動；其餘卡牌涵蓋攻擊擊殺記錄、屍體驅動的手牌與戰場效果、
符文篩選發現、移除牌堆手下、友方不死族死亡觸發和全場消滅。

牌組選擇器也可以在內建 AI 與「本機雙人」間切換。練習 AI 提供「簡單/普通/困難」三檔：簡單檔
刻意採用樸素選擇，普通檔會規劃斬殺與法力，困難檔還會換掉高費起手並優先有利交換。三檔都只讀取
目前玩家投影與權威合法操作，保持確定性，並隨自動存檔/續局恢復。熱座對戰分別使用玩家 1/玩家 2 的牌組，
並會在起手換牌、每個回合以及其他輸入權切換前顯示全螢幕隱私交接頁；下一位玩家確認準備好之前，
手牌與卡牌預覽保持隱藏，回合計時也會暫停。命令列可用 `--hotseat` 直接選擇此模式，
並可與 `--quick-start` 組合。

每場新的圖形對戰都會依種子確定性隨機選擇先手，且不會消耗卡牌效果使用的隨機流。先手換 3 張，
後手換 4 張，並在雙方確認後獲得幸運幣；換牌頁、熱座交接頁與對戰側欄都會明確顯示先後手。
replay 與 snapshot 證明資料會保存該結果，舊檔案缺少此欄位時則相容地維持玩家 1 先手。

每個構築職業現在都會使用對應的標準基礎英雄身分，從卡爾洛斯一直到巫妖王，同時仍以所選牌組的
英雄能力為權威。英雄名稱與預覽跟隨目前語言；基礎英雄頭像會保留為可收藏中繼資料，但不會誤佔
主牌組卡位，可加入牌組的英雄牌則不受影響。

長局遵循官方構築對戰上限：先手完成自己的第 45 個回合（對戰回合 89）並結算完回合結束效果後，
若仍未分出勝負則判為平手。第 90 回合不會開始，因此也不會抽牌或觸發回合開始效果；結果橫幅會用
目前語言明確提示這是達到回合上限的平手。

對戰內建 AI 時，電腦的合法操作會以短暫可見間隔逐步播放，不再同步瞬間完成整個回合。動作面板
會顯示電腦正在思考，戰鬥回饋可以在兩次決策之間完整呈現，且每一步 AI 操作都會以原子方式
自動儲存；在 AI 回合中暫停或重新啟動，也會從最後一個已完成操作安全繼續。

對戰中點擊「暫停」或按 `Esc` 會開啟對戰內選單。選單開啟期間，以及從選單進入設定頁期間，回合
計時、AI 逐步行動、瞄準指引與戰鬥表現都會停止推進。選單可繼續對戰、進入設定後返回、儲存並
回到主選單，或經過獨立二次確認後投降。即使目前輸入權屬於 AI，投降也一律作用於本機玩家，並
寫入確定性重播記錄。

對戰控制區也提供六種常見英雄表情：感謝、打得不錯、問候、驚嘆、失誤與威脅。本地化氣泡會自動
收起，並帶有短暫防刷冷卻；內建 AI 會延遲給出確定性回應。「屏蔽對手」會在本局剩餘時間隱藏這些
回應；熱座模式中，屏蔽狀態分別屬於目前觀察者，不會洩漏給另一位玩家。

未完成對戰會在每次合法操作後以原子方式自動儲存。對戰內按「暫停」、主選單按「繼續對戰」
即可稍後恢復；用戶端重新啟動後也會從經過重播驗證的同一狀態繼續。私有存檔預設位於
`$XDG_STATE_HOME/hearth-rs/active-match.json`（未設定 XDG 時為
`~/.local/state/hearth-rs/active-match.json`），Unix 上僅允許目前使用者讀寫，對戰結束後自動刪除。
它包含雙方隱藏區域，因此不會作為一般介面匯出。`--resume PATH` 可使用隔離存檔，
`--no-resume` 可停用磁碟持久化；熱座對戰恢復時一律先回到隱私交接頁，交接頁也可安全暫停
返回主選單。「放棄已儲存對戰」必須再次確認後才會刪除存檔。

對戰介面使用原創且內嵌的繪製風格酒館棋盤與不暴露身分的對手卡背。兩張素材會直接編譯進
執行檔，紋理載入期間仍有純色降級顯示，且不包含從官方遊戲擷取的美術或商標。生成提示詞與
來源記錄在[介面素材說明](crates/hearth-client-bevy/assets/ui/README.md)中。

戰鬥表現只會讀取目前觀察者可見的公開事件流。可見角色在攻擊、受擊、治療、召喚、格擋與變形時
會產生短促脈衝；傷害、治療、護甲、傷害免疫、凍結及死亡會顯示本地化浮字。攻擊與指向性效果也
會沿帶色彩的來源至目標軌跡移動；英雄能力等非場上來源會安全映射至其控制者英雄。致死目標若已
離場，提示會安全退回戰場中央，不會為動畫讀取權威隱藏狀態。動作送出前，點擊與拖曳指向性來源
都會顯示金色瞄準箭頭；停留於合法可見目標時箭頭會變紅並吸附，動作完成、場景切換或熱座隱私
交接時會立即清除。選擇或拖曳可打出的手下/地標時，己方戰場會顯示全部合法插入位置；可直接點擊
位置或把卡牌精確拖入。若戰吼仍需目標，已選站位會保留，玩家可以繼續瞄準目標。
玩家可見的公開事件會立即寫入右側戰鬥紀錄，不再透過頂部懸浮視窗排隊播放。

執行經典洞穴任務賊對局：

```bash
cargo run -p hearth-cli -- play \
  --deck-one decks/quest_rogue.json \
  --deck-two decks/quest_rogue.json \
  --locale zhCN \
  --seed 42
```

牌表採用 Dog 在 2017 年公開使用的 30 張構築，包含探索地下洞穴、水晶核心、回手元件、帕奇斯、莫羅斯和紫羅蘭教師。任務進度、回手減費、下一個法術減費、牌庫招募、隨機異職業牌和 5/5 持續效果均由 Lua 卡牌/關鍵詞模組實現；Rust 只新增了通用起手規則和裝備生成武器原語。

普通牌組會校驗本職業/中立卡。遊客牌在 Lua 中宣告 `deck_allowances`，可開放指定職業與
卡包並排除目標職業的遊客牌；規則展示用的 `demo.json` 顯式設定 `unrestricted: true`，
因為它有意混合多個職業來演示機制。

雙方依次輸入 `keep` 完成排程。常用命令：

```text
state                       檢視場面
hand                        檢視當前玩家手牌
cards                       檢視卡牌包
legal                       列出所有合法行動
targets <手牌實體ID>         檢視目標
play <手牌實體ID> [目標ID]   出牌
trade <手牌實體ID>           花費1點法力交易可交易牌
action <實體ID> <動作ID> [目標ID] 執行鍛造、預備或泰坦能力
attack <攻擊者ID> <目標ID>   攻擊
power [目標ID]              使用英雄技能
choose <編號>               完成發現/選擇
end                         結束回合
save <檔案>                 儲存 replay
snapshot <檔案>             儲存狀態快照
```

## 玩家控制器與隱藏資訊

兩個玩家位置都可以獨立選擇 `interactive`、`bot` 或 `fuzzer`：

```bash
cargo run -p hearth-cli --release -- play \
  --deck-one decks/quest_rogue.json \
  --deck-two decks/quest_rogue.json \
  --player-one interactive \
  --player-two bot
```

控制器只能接收玩家視角投影和引擎給出的合法操作後設資料，不能讀取原始 `GameState`。玩家視角會排除雙方牌庫順序（也包括自己的牌庫順序）、對手手牌和普通秘密身分、指令碼資料、隱藏光環來源、RNG 狀態以及 replay；任務、任務線和支線任務則按官方規則保持公開。CLI 事件輸出會隱藏對手抽牌、產生到手牌的卡、未揭示秘密名稱、隱藏選擇和隱藏隨機抽樣。雙互動玩家熱座模式使用清屏交接；普通對局中禁止匯出包含權威隱藏狀態的 replay/snapshot，只有明確傳入 `--debug-state` 才會開啟該除錯能力。

基礎 [`hearth-bot`](crates/hearth-bot/README.md) 按「場攻斬殺、規劃目前合法操作以儘量打滿費用、優勢交換、打臉」的順序行動。嘲諷等攻擊限制仍完全由引擎決定，因為 Bot 只會從引擎列舉的合法攻擊中選擇。

## 驗證

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

端到端測試會載入真實 Lua 卡包，將每個 Lua 卡牌 ID 與來源快照逐一比對，並驗證 68 項關鍵詞目錄、set、關鍵詞 Lua 規則、戰吼、亡語、奧秘、磁力、鍛造、預備、發現、衍生物、replay 和 snapshot。

## 狀態機 Fuzz 測試

確定性狀態機 Fuzzer 實作在 [`hearth-fuzz` 函式庫](crates/hearth-fuzz/README.md)中，並且只透過 `hearth-cli` 子命令提供；普通 `cargo test` 不會啟動 fuzz campaign。它會產生職業合法牌組、抽取引擎列舉的合法操作、在每一步校驗狀態不變量，並將最終狀態與 replay 比對：

```bash
cargo run -p hearth-cli --release -- fuzz --seeds 100 --steps 180
```

## 邊界

這仍是規則原型，不是完整爐石服務端。關鍵詞層已經覆蓋構築模式詞表，但卡牌庫仍是 1999 個官方定義，並不等於完整官方卡池；酒館戰棋、傭兵戰紀的模式專屬關鍵詞也不在本 CLI 對戰規則範圍內。新增一種現有鉤子無法描述的基礎規則時，應優先增加通用規則鉤子或原子效果，而不是在 Rust 中判斷具體卡牌或關鍵詞名。
